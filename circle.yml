machine:
  timezone:
    Asia/Tokyo
  ruby:
    version:
      2.5.1

dependencies:
  override:
    - bundle install --path vendor/bundle:
        timeout: 300

database:
  override:

test:
  override:
    - bundle exec rspec spec:
        environment:
          RAILS_ENV: test

  post:
    - echo "2nd Time"
    - bundle exec rspec spec:
        environment:
          MODE: test
